# RABL vs. ActiveModel Serializers

Performance benchmark of RABL and ActiveModel Serializers for
JSON serialization.

## Setup

1. Install required gems

        bundle install

2. Run the benchmark

        bundle exec ruby benchmark.rb
